machine:
  services:
    - elasticsearch
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.3.0

dependencies:
  pre:
    - cp dot.env .env

test:
  override:
    - bundle exec rspec:
        parallel: true
        files:
          - spec/**/*_spec.rb
