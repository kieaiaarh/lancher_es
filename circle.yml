machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.1.7
deployment:
  master:
    branch: master
      commands:
        - bundle exec cap production deploy
        - bundle exec cap production deploy:restart
